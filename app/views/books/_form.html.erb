<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :book_title %>
  </div>

  <div class="field">
    <%= form.label :arthur %>
    <%= form.text_field :arthur, id: :book_arthur %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :book_content %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :book_price %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :book_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
